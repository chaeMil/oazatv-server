{block content}

<div class="container">
    <div class="row">
        <div class="col-xs-12">
            {if isset($newestAlbums)}
                <div class="row videos-box">
                    {foreach $newestAlbums as $item}
                        {include '../../Shared/videoBox.latte', item => $item}
                    {/foreach}
                </div>
            {/if}                    
        </div>
    </div>
</div>